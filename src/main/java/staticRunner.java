public class staticRunner {

    /*
    static is used for the memory management and create an reference during the compiler runs
    instance method calls usually create an object everytime
    static goes anyplace , non static never goes outside of static
    static variable value can be changed and its not constant
    static final is an equal to constant
    cannot use super in  static context
    static variables and methods called using the classname as preferred way.
    here object reference not created.
    Static methods give functionality to the class itself,
    while non-static methods give functionality to an object of that class.

     */

    public static void main(String[] args) {

        staticMethodTest.defaultruncheck();

    }

}
